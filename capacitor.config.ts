import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.adminsismais2',
  appName: 'adminsismais2',
  webDir: 'dist'
};

export default config;
